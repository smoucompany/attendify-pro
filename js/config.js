/* =========================================================
   ATTENDIFY PRO — App Configuration
   ========================================================= */

const AppConfig = {
  // Backend على نفس الدومين (Vercel Serverless)
  backend: {
    url: '',
  },
};
